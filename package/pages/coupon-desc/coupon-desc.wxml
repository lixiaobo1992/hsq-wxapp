<!--pages/coupon-desc/coupon-desc.wxml-->
<view class="page">
  <view class="about">关于说明</view>
  <view class="desc" wx:for="{{list}}" wx:key="{{item}}">
    <view class="index">{{index+1}}.</view>
    <view class="item">{{item}}</view>
  </view>
</view>